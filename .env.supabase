# Copy this file to ".env" and fill in your real values before running the app.

# Port the web server runs on
PORT=3000

# Random long random string used to sign sessions and response-link tokens.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=2e13dc0a4f4de47f00e41183a6c9ceaee73ca4b6f9df27239cfd94346c6816a1
TOKEN_SECRET=cdfb4cb870672b8a3976aa00eec7ec355e9e3e3c7e287ad5beed00719c9f1136

# Public base URL of your deployed site (used inside email links).
# For local testing this is fine as-is.
BASE_URL=http://localhost:3000

# --- Supabase Configuration ---
# Get these from your Supabase project settings -> API
SUPABASE_URL=https://mpvmzwxsvufbfzhelwri.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1wdm16d3hzdnVmYmZ6aGVsd3JpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODk5MDU1OTUsImV4cCI6MjEwNTQ4MTU5NX0.g-zUmK-OcCXvHyCTBSlLFV4yH3fhbi4Jb3csKhckUAM
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1wdm16d3hzdnVmYmZ6aGVsd3JpIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4OTkwNTU5NSwiZXhwIjoyMTA1NDgxNTk1fQ.yj-e_rTAzJWa2kYjkDHrW9POz4Zkd7JJR_Vs541qaRc

# Aladhan calculation method id (1 = University of Islamic Sciences, Karachi - good default for Pakistan)
# Full list: https://aladhan.com/prayer-times-api  (see "Methods")
PRAYER_CALC_METHOD=1

# --- Email (SMTP) settings ---
# Any SMTP provider works: Gmail (with an App Password), SendGrid, Mailtrap, Resend SMTP, etc.
# IMPORTANT: You must configure these for email invitations to work!
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=hk09812367@gmail.com
SMTP_PASS=lltaaspyfysvdnmc
EMAIL_FROM="Prayer Reminder <hk09812367@gmail.com>"

# How many minutes after the prayer time to send the "Did you pray?" follow-up email
FOLLOWUP_DELAY_MINUTES=45

# How many minutes after the prayer time to auto-mark a prayer "missed" if no response
MISSED_AFTER_MINUTES=180

# --- Production Cron Job Settings ---
# Set this to use external cron services (Vercel Cron, GitHub Actions, etc.) instead of internal node-cron
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# If set, the internal scheduler is disabled and you must call POST /api/cron/prayer-scheduler with Bearer token
# Leave empty for development (uses internal node-cron scheduler)
CRON_SECRET=